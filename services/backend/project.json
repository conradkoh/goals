{
  "name": "@services/backend",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "services/backend",
  "projectType": "library",
  "targets": {
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm dev",
        "cwd": "services/backend"
      },
      "continuous": true
    },
    "deploy": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm deploy",
        "cwd": "services/backend"
      }
    }
  }
}
