{
  "name": "goals",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*",
      "services/*",
      "apps/*"
    ],
    "nohoist": [
      "**/convex-test",
      "**/convex-test/**",
      "**/convex",
      "**/convex/**",
      "**/convex-helpers",
      "**/convex-helpers/**"
    ]
  },
  "scripts": {
    "install:clean": "find . -name 'node_modules' -type d -prune -exec trash {} + && yarn install"
  }
}
